import { SearchBarWrapper } from "./SearchBar.styles";

const SearchBar = () => {
  return (
    <SearchBarWrapper>
      <div className="banner-img">
        <div></div>
      </div>
    </SearchBarWrapper>
  );
};

export default SearchBar;
